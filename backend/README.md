# Backend

FastAPI, SQLAlchemy and PostgreSQL API for Subscription Tracker. From this directory run `pip install -r requirements.txt`, `alembic upgrade head`, then `uvicorn app.main:app --reload`. Run the isolated SQLite test suite with `pytest`.
